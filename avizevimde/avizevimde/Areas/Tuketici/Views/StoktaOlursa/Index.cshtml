@model IEnumerable<avizevimde.Areas.Tuketici.Models.stoktaOlursa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.urun.alt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.eklenmeTarihi)
        </th>
        <th>
            @Html.HiddenFor(model => model.tuketiciId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.urun.alt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.eklenmeTarihi)
        </td>
        <td>
            @Html.HiddenFor(modelItem => item.tuketiciId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
